defmodule Panelist do
    defstruct [:email_address, :gender, :postal_code, :date_of_birth, :member_id, :first_name, :last_name] # , :phone_number, :street_address, :payment_method_id, :recruitment_source, :variables, :tracking_consent]
end
